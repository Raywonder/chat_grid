# Windows native client

Windows-specific packaging, the Inno Setup installer, and the official NVDA
Controller Client redistribution live in this tree. Shared wxPython application
code remains in `../src`.

Build from PowerShell with `./scripts/build-windows.ps1`.
